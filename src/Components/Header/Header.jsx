import styles from './Header.module.scss';

export const Header = () => {

    return (
        <header className={styles.HeaderContainer}>
            <h2>Test</h2>
      </header>
    )
}

export default Header